version: 2
jobs:
    build:
        machine: true
        steps:
            - checkout
            - run:
                  name: Build image
                  command: docker build -t gusdecool/symfony .
