jobs:
    build:
        steps:
            - checkout
            - setup_remote_docker
            - run:
                  name: Build image
                  command: docker build -t gusdecool/symfony .
